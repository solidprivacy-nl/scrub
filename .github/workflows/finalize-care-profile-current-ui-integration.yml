name: Finalize Zorgfilter current UI integration

on:
  push:
    branches:
      - scrub-care-profile-current-ui-integration
    paths:
      - .github/workflows/finalize-care-profile-current-ui-integration.yml
      - ops/finalize_care_profile_current_ui_integration.py

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: scrub-care-profile-current-ui-integration
          fetch-depth: 0

      - name: Apply governance finalization
        run: python ops/finalize_care_profile_current_ui_integration.py

      - name: Commit finalization
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No governance changes to commit"
            exit 1
          fi
          git commit -m "Finalize Zorgfilter current UI integration governance"
          git push origin HEAD:scrub-care-profile-current-ui-integration
