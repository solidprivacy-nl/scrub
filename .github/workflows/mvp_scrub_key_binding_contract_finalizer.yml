name: MVP Scrub Key binding contract finalizer

on:
  push:
    branches:
      - scrub-mvp-scrub-key-binding-contract-tests

permissions:
  contents: write

jobs:
  finalize:
    if: github.event.head_commit.message != 'Finalize Scrub Key binding contract tests'
    runs-on: ubuntu-latest
    steps:
      - name: Check out contract branch
        uses: actions/checkout@v4
        with:
          ref: scrub-mvp-scrub-key-binding-contract-tests
          fetch-depth: 0

      - name: Apply governance finalisation
        run: python scripts/finalize_mvp_scrub_key_binding_contract_tests.py

      - name: Remove one-time finalizer and generated cache
        shell: bash
        run: |
          rm -f scripts/finalize_mvp_scrub_key_binding_contract_tests.py
          find . -type d -name __pycache__ -prune -exec rm -rf {} +
          find . -type f -name '*.pyc' -delete

      - name: Commit finalised contract package
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'solidprivacy-binding-contract-worker'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git commit -m 'Finalize Scrub Key binding contract tests'
          git push origin HEAD:scrub-mvp-scrub-key-binding-contract-tests
