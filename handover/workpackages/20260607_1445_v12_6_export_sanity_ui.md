# Handover — WP3B — v12.6 Export sanity checks UI integration

Repository: solidprivacy-nl/scrub  
Status: UI integration implemented; awaiting GitHub Actions, Hugging Face sync and app verification

## Summary

Integrated the existing v12.6 export sanity helper into the existing Streamlit startup patch flow. The app should now show an `Extra exportcontrole` block near `Eindcontrole vóór download` and before the download/export section. The warnings remain advisory only: downloads remain available and export settings remain unchanged.

## Files added

- `tests/test_export_sanity_ui_patch.py`
- `handover/workpackages/20260607_1445_v12_6_export_sanity_ui.md`

## Files changed

- `fix_streamlit_nested_expanders.py`
- `WORKPACKAGES.md`

## Tests

- Added patch-level test coverage in `tests/test_export_sanity_ui_patch.py`.
- Tests check that:
  - the export sanity helper is imported;
  - `Extra exportcontrole` is present;
  - the download section marker remains present;
  - `apply_replacements_to_text` is not altered by the patch;
  - download buttons are not removed by the patch;
  - warning behavior remains advisory and non-blocking.

## Validation

- Local pytest: not run from this connector environment.
- Required validation still pending:
  - `PYTHONPATH=. pytest -q tests/test_export_sanity.py`
  - `PYTHONPATH=. pytest -q tests/test_review_summary.py`
  - `PYTHONPATH=. pytest -q tests/test_export_sanity_ui_patch.py`
  - preferably `PYTHONPATH=. pytest -q`
- GitHub Actions: pending for the UI integration commits.
- Hugging Face sync: pending for the UI integration commits.
- App verification: pending; UI behavior changed and should be visually checked in Hugging Face.

## Coordinator-provided prerequisite evidence

- Helper/reconciliation baseline reported by coordinator:
  - `Tests #58` green.
  - `Sync to Hugging Face Space #72` green.
  - commit `b0bf8ae`.

## Commits created in this workpackage

- `c60b9b4bfa8944e620546ca26a4fe42c287edaa0` — Integrate export sanity warnings into UI patch.
- `f5158c9faf8e7676cb8403da0b42b0465539acfa` — Add export sanity UI patch tests.
- `7d043d13096518d5dca6a5f187189fa3a8471627` — Update workpackage status for export sanity UI.

## Notes / risks

- `presidio_streamlit.py` was not directly edited.
- Export/download code was not changed directly.
- TXT, CSV, DOCX and PDF download behavior should remain unchanged, but app verification is still required.
- The connector workflow/status endpoints may not expose the Actions/sync runs; coordinator may need to verify in GitHub UI.
- `CHANGELOG.md` still needs a final WP3B entry if not completed by a follow-up status worker.

## Next recommended step

- Verify GitHub Actions and Hugging Face sync for the UI integration commits, then open the Hugging Face app and confirm that `Extra exportcontrole` appears before downloads and that downloads remain available.
