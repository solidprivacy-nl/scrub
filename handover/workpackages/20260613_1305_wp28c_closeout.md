# Handover — WP28C-CLOSEOUT

Repository worked in: `solidprivacy-nl/scrub`

Workpackage title: `WP28C-CLOSEOUT — Scrub Key warning/reinsert evidence closeout`

Status: blocked; full app evidence for WP28C is incomplete.

## Summary

Attempted WP28C closeout after coordinator requested the next safe step. The closeout was not completed because the available evidence does not yet verify the full WP28C app behavior.

The current coordinator screenshot shows recent green GitHub Actions / Sync to Hugging Face Space rows and the running app in the normal Scrub Legal anonymization/review flow. It does not show the required `Originele waarden terugzetten` / Scrub Key reinsert mode surfaces.

Existing repository evidence at `WP28C_APP_EVIDENCE_SCRUB_KEY_WARNING_UI.md` explicitly records only partial app evidence and says WP28C still needs full closeout evidence for Actions, Hugging Face sync and all expected warning/acknowledgement surfaces.

No product code, tests, UI behavior, export/download behavior, Scrub Key behavior, reinsert behavior, dependencies, cloud processing or real-data fixtures were changed.

## Files added

- `workpackage_claims/WP28C_CLOSEOUT.md`
- `handover/workpackages/20260613_1305_wp28c_closeout.md`

## Files changed

- `workpackage_claims/WP28C_CLOSEOUT.md`

## Tests/checks run

Documentation/status checks through GitHub connector:

- Read required central files: `PROJECT_PROMPT.md`, `ROADMAP.md`, `WORKPACKAGES.md`, `CHANGELOG.md`.
- Read WP28C implementation claim: `workpackage_claims/WP28C_mvp_scrub_key_warning_acknowledgement_ui.md`.
- Read WP28C implementation handover: `handover/workpackages/20260612_1545_mvp_scrub_key_warning_acknowledgement_ui.md`.
- Read prior WP28C verify claims and handovers:
  - `workpackage_claims/WP28C_VERIFY_github_actions_huggingface_app_closeout.md`
  - `handover/workpackages/20260612_1600_wp28c_verify_actions_sync_app_closeout.md`
  - `workpackage_claims/WP28C_VERIFY_warning_acknowledgement_ui_closeout.md`
  - `handover/workpackages/20260612_1605_wp28c_warning_acknowledgement_ui_verify.md`
- Read app evidence file: `WP28C_APP_EVIDENCE_SCRUB_KEY_WARNING_UI.md`.
- Used compare history to identify the relevant WP28C evidence and claim files.

No shell/pytest was run; this was verification/documentation-only.

## Validation status

Not completed.

What is available:

- WP28C implementation claim exists and is marked completed.
- WP28C implementation handover exists.
- Partial app evidence exists for Scrub Key/reinsert warning UI.
- The latest coordinator screenshot shows recent green Actions/sync and normal app startup.

What is still missing for closeout:

- A coordinator screenshot or equivalent app evidence in mode `Originele waarden terugzetten` showing the Scrub Key / reinsert warning and acknowledgement surfaces required by WP28C.
- Confirmation that the relevant buttons are gated by acknowledgement/input state in the running app.
- Confirmation that after acknowledgement, JSON/restored content, filenames and MIME types remain unchanged.

## GitHub Actions status

Recent green Actions are visible in the coordinator screenshot, but this evidence is tied to the latest repository state, not specifically to a WP28C reinsert-mode app verification screenshot.

## Hugging Face sync status

Recent green Sync to Hugging Face Space is visible in the coordinator screenshot, but full WP28C closeout still requires the reinsert-mode app verification screenshot.

## App verification status

Blocked / incomplete.

The required app verification points from the WP28C handover remain:

```text
Scrub Key JSON download disabled until export acknowledgement is checked.
Scrub Key import/load disabled until import acknowledgement is checked.
Pasted-text/TXT/DOCX/PDF-to-TXT reinsert buttons disabled until acknowledgement is checked.
Restored output downloads disabled until download acknowledgement is checked.
After acknowledgement, JSON/restored content, filenames and MIME types remain unchanged.
Existing anonymization/export flow still works.
```

## Remaining risks

- UI acknowledgements are safety prompts only, not encryption, protected storage, automatic deletion or key recovery.
- Full WP28C closeout should not be marked completed until the reinsert-mode screenshot/evidence is available.

## Next recommended step

Provide a screenshot of the running Hugging Face app in mode:

```text
Originele waarden terugzetten
```

The screenshot should show the Scrub Key warning / acknowledgement surfaces and the reinsert warning / acknowledgement surfaces. Then rerun:

```text
WP28C-CLOSEOUT
```

Alternative safe next package while waiting for evidence:

```text
WP39B — DOCX hygiene audit UI planning
```
