# MVP product quality gate

Status: active roadmap gate.

The product priority is now the core MVP workflow:

```text
Import -> Scrub -> Review -> Replace -> Scrub Key -> Reinsert -> Export -> Audit
```

Phase 7 pilot/commercial follow-up is parked until the coordinator confirms that this workflow is credible enough for external validation.

WP50 and WP51 remain early thinking artifacts, but they are not the active execution line.

Near-term work should focus on product quality, especially DOCX hygiene, document-centric review and easier replace/review logic.

This file changes planning priority only. It does not change code, UI, export behavior, Scrub Key behavior or runtime behavior.
